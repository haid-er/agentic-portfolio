'use client'
/** GHG calculator — STUB. Owner: see CONTRACTS.md (components/demos/ghg-calculator/**). */
import { EmptyState } from '@/components/ui/States'
import type { DemoProps } from '@/lib/demos/types'

export { notes } from './notes'

export default function Demo(_props: DemoProps) {
  return <EmptyState title="GHG calculator">This demo is being built.</EmptyState>
}
