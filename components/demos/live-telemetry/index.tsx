'use client'
/** Live telemetry — STUB. Owner: see CONTRACTS.md (components/demos/live-telemetry/**). */
import { EmptyState } from '@/components/ui/States'
import type { DemoProps } from '@/lib/demos/types'

export { notes } from './notes'

export default function Demo(_props: DemoProps) {
  return <EmptyState title="Live telemetry">This demo is being built.</EmptyState>
}
