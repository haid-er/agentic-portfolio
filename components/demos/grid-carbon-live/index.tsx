'use client'
/** Grid carbon live — STUB. Owner: see CONTRACTS.md (components/demos/grid-carbon-live/**). */
import { EmptyState } from '@/components/ui/States'
import type { DemoProps } from '@/lib/demos/types'

export { notes } from './notes'

export default function Demo(_props: DemoProps) {
  return <EmptyState title="Grid carbon live">This demo is being built.</EmptyState>
}
