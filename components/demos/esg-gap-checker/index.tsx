'use client'
/** ESG disclosure gap checker — STUB. Owner: see CONTRACTS.md (components/demos/esg-gap-checker/**). */
import { EmptyState } from '@/components/ui/States'
import type { DemoProps } from '@/lib/demos/types'

export { notes } from './notes'

export default function Demo(_props: DemoProps) {
  return <EmptyState title="ESG disclosure gap checker">This demo is being built.</EmptyState>
}
