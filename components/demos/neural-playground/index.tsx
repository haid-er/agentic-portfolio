'use client'
/** Neural playground — STUB. Owner: see CONTRACTS.md (components/demos/neural-playground/**). */
import { EmptyState } from '@/components/ui/States'
import type { DemoProps } from '@/lib/demos/types'

export { notes } from './notes'

export default function Demo(_props: DemoProps) {
  return <EmptyState title="Neural playground">This demo is being built.</EmptyState>
}
