'use client'
/** Ownership extractor — STUB. Owner: see CONTRACTS.md (components/demos/ownership-extractor/**). */
import { EmptyState } from '@/components/ui/States'
import type { DemoProps } from '@/lib/demos/types'

export { notes } from './notes'

export default function Demo(_props: DemoProps) {
  return <EmptyState title="Ownership extractor">This demo is being built.</EmptyState>
}
