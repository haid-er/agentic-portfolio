'use client'
/** Stripe Connect flow — STUB. Owner: see CONTRACTS.md (components/demos/stripe-connect-flow/**). */
import { EmptyState } from '@/components/ui/States'
import type { DemoProps } from '@/lib/demos/types'

export { notes } from './notes'

export default function Demo(_props: DemoProps) {
  return <EmptyState title="Stripe Connect flow">This demo is being built.</EmptyState>
}
