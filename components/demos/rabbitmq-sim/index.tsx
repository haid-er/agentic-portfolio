'use client'
/** RabbitMQ broker sim — STUB. Owner: see CONTRACTS.md (components/demos/rabbitmq-sim/**). */
import { EmptyState } from '@/components/ui/States'
import type { DemoProps } from '@/lib/demos/types'

export { notes } from './notes'

export default function Demo(_props: DemoProps) {
  return <EmptyState title="RabbitMQ broker sim">This demo is being built.</EmptyState>
}
