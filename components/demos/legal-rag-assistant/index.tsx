'use client'
/** Legal RAG assistant — STUB. Owner: see CONTRACTS.md (components/demos/legal-rag-assistant/**). */
import { EmptyState } from '@/components/ui/States'
import type { DemoProps } from '@/lib/demos/types'

export { notes } from './notes'

export default function Demo(_props: DemoProps) {
  return <EmptyState title="Legal RAG assistant">This demo is being built.</EmptyState>
}
