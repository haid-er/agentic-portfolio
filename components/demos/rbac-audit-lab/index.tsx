'use client'
/** RBAC + audit lab — STUB. Owner: see CONTRACTS.md (components/demos/rbac-audit-lab/**). */
import { EmptyState } from '@/components/ui/States'
import type { DemoProps } from '@/lib/demos/types'

export { notes } from './notes'

export default function Demo(_props: DemoProps) {
  return <EmptyState title="RBAC + audit lab">This demo is being built.</EmptyState>
}
