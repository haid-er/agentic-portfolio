'use client'
/** CI/CD pipeline — STUB. Owner: see CONTRACTS.md (components/demos/ci-pipeline/**). */
import { EmptyState } from '@/components/ui/States'
import type { DemoProps } from '@/lib/demos/types'

export { notes } from './notes'

export default function Demo(_props: DemoProps) {
  return <EmptyState title="CI/CD pipeline">This demo is being built.</EmptyState>
}
