'use client'
/** SQL playground — STUB. Owner: see CONTRACTS.md (components/demos/sql-playground/**). */
import { EmptyState } from '@/components/ui/States'
import type { DemoProps } from '@/lib/demos/types'

export { notes } from './notes'

export default function Demo(_props: DemoProps) {
  return <EmptyState title="SQL playground">This demo is being built.</EmptyState>
}
