'use client'
/** Vector space explorer — STUB. Owner: see CONTRACTS.md (components/demos/vector-space-explorer/**). */
import { EmptyState } from '@/components/ui/States'
import type { DemoProps } from '@/lib/demos/types'

export { notes } from './notes'

export default function Demo(_props: DemoProps) {
  return <EmptyState title="Vector space explorer">This demo is being built.</EmptyState>
}
