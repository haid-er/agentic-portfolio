'use client'
/** Algorithm visualiser — STUB. Owner: see CONTRACTS.md (components/demos/algo-visualizer/**). */
import { EmptyState } from '@/components/ui/States'
import type { DemoProps } from '@/lib/demos/types'

export { notes } from './notes'

export default function Demo(_props: DemoProps) {
  return <EmptyState title="Algorithm visualiser">This demo is being built.</EmptyState>
}
