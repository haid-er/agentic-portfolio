'use client'
/** Ask the portfolio — STUB. Owner: see CONTRACTS.md (components/demos/ask-malik/**). */
import { EmptyState } from '@/components/ui/States'
import type { DemoProps } from '@/lib/demos/types'

export { notes } from './notes'

export default function Demo(_props: DemoProps) {
  return <EmptyState title="Ask the portfolio">This demo is being built.</EmptyState>
}
