'use client'
/** Org-chart extractor — STUB. Owner: see CONTRACTS.md (components/demos/org-chart-extractor/**). */
import { EmptyState } from '@/components/ui/States'
import type { DemoProps } from '@/lib/demos/types'

export { notes } from './notes'

export default function Demo(_props: DemoProps) {
  return <EmptyState title="Org-chart extractor">This demo is being built.</EmptyState>
}
