'use client'
/** System design canvas — STUB. Owner: see CONTRACTS.md (components/demos/system-design-canvas/**). */
import { EmptyState } from '@/components/ui/States'
import type { DemoProps } from '@/lib/demos/types'

export { notes } from './notes'

export default function Demo(_props: DemoProps) {
  return <EmptyState title="System design canvas">This demo is being built.</EmptyState>
}
