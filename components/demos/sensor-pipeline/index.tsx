'use client'
/** Sensor pipeline — STUB. Owner: see CONTRACTS.md (components/demos/sensor-pipeline/**). */
import { EmptyState } from '@/components/ui/States'
import type { DemoProps } from '@/lib/demos/types'

export { notes } from './notes'

export default function Demo(_props: DemoProps) {
  return <EmptyState title="Sensor pipeline">This demo is being built.</EmptyState>
}
