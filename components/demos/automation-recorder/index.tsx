'use client'
/** Automation recorder — STUB. Owner: see CONTRACTS.md (components/demos/automation-recorder/**). */
import { EmptyState } from '@/components/ui/States'
import type { DemoProps } from '@/lib/demos/types'

export { notes } from './notes'

export default function Demo(_props: DemoProps) {
  return <EmptyState title="Automation recorder">This demo is being built.</EmptyState>
}
