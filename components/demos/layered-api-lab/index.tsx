'use client'
/** Layered API lab — STUB. Owner: see CONTRACTS.md (components/demos/layered-api-lab/**). */
import { EmptyState } from '@/components/ui/States'
import type { DemoProps } from '@/lib/demos/types'

export { notes } from './notes'

export default function Demo(_props: DemoProps) {
  return <EmptyState title="Layered API lab">This demo is being built.</EmptyState>
}
