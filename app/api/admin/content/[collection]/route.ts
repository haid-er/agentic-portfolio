/** POST /api/admin/content/[collection] — validate + save. STUB — owner: admin-core. */
export async function POST(): Promise<Response> {
  return Response.json({ error: 'not implemented' }, { status: 501 })
}
